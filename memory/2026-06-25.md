# Memory — 2026-06-24 ~ 2026-06-25

## Massive net4.xyz Website Upgrade Session

### Key Person
- User: "飘叔" (piaoshu), founder of net4.xyz, GitHub: piaoshuweb3, Vercel: piaoshuweb3-9227s-projects

### Project Context
- Workspace: `D:\F盘同步\Web4.0应用开发专区\net4.xyz  6.14完成版`
- Monorepo (pnpm workspaces: packages/frontend + packages/backend + packages/shared)
- Frontend: Next.js 14 App Router, React 18, TailwindCSS, Three.js
- Backend: NestJS with MongoDB/Prisma (has TS errors from ungenerated Prisma client)
- Wallet: Wagmi + RainbowKit + Coinbase MPC Wallet
- Git remote: https://github.com/piaoshuweb3/net4.xyz

### SEO/AEO Overhaul (2026-06-24 18:11-18:41)
- Added 11 files for search engine / AI agent optimization
- robots.ts with rules for 11 AI agent crawlers (GPTBot, Claude, Perplexity, etc.)
- sitemap.ts with 22 URLs (home, blockchain, wallet, 11 eco subdomains, 8 articles)
- .well-known/agent.json — AI agent discovery endpoint
- llms.txt + llms-full.txt — full site content for LLMs (5600+ words)
- JSON-LD structured data (Website + Organization + WebApplication + FAQPage)
- layout metadata: canonical URL, OG images, Twitter cards, googleBot directives
- All 5 endpoints return 200

### Critical: Vercel Build Module Resolution
**Vercel cannot resolve `@/` path aliases in the monorepo build environment**, even though local dev works fine with `moduleResolution: "bundler"` in tsconfig.
- Fix: Converted ALL `@/` imports (18 files) to relative imports using a Node.js script
- This was the root cause of ~20 failed deployments
- After conversion, all builds succeeded consistently

### Vercel Configuration
- Project: `net4-xyz` under team `piaoshuweb3-9227s-projects`
- Root Directory: `packages/frontend`
- Build Command: `next build`
- Install Command: `cd ../.. && pnpm install --frozen-lockfile`
- sourceFilesOutsideRootDirectory: true
- Domain: `net4.xyz` verified but DNS still pointing to Docker IP (172.19.3.22) — user said they'd update DNS
- Token: [Vercel token stored in env — do not commit]

### Admin System
- Admin address: `0x10687368eF1be3f178de0fCCf5EdfF49e1C258B1` stored as env var `NEXT_PUBLIC_ADMIN_ADDRESS`
- Admin auth utility: `utils/admin-auth.ts` with `isAdmin()` / `checkAdminAccess()`
- Admin nav link is wallet-gated — only visible when wallet is connected
- 8 admin subpages: dashboard, users, content, membership, finance, nodes, stats, settings
- AdminLayout inlined in `app/admin/layout.tsx` (Vercel build workaround)

### Phase 1 & 2: Brand Upgrade (2026-06-24 23:55 → 2026-06-25 00:06)
- **ParticleBackground**: Fullscreen Canvas particle network with mouse interaction
- **LiveStatsTicker**: Mock real-time data (AI avatars, tasks, computing load, AFC market cap)
- **CTECycle**: Create-Task-Earn economic loop visualization with auto-rotation
- **TrustSection**: Audit badges + 4-phase roadmap timeline
- **Web4Globe**: Interactive Three.js 3D globe with 11 ecosystem nodes, orbit rings, click/hover detection, 2 atmosphere glow shader

### Phase 3: AI Avatar Dashboard (2026-06-25 00:06)
- Route `/avatar` with full layout + metadata
- 3-step creation wizard (name → skills → confirm)
- Avatar cards with status dots (online/idle/offline)
- Stats overview (total/online/tasks/earnings)
- Detail panel (cognitive ability, earnings analysis, actions)
- 10 skill options, 6 roles, mock data with 3 sample avatars

### Phase 4: Digital Labor Market (2026-06-25 01:10)
- Route `/labor` — task feed with 8 mock tasks
- Category filter (all/blockchain/AI/dev/data analysis/content)
- Difficulty tags (beginner/intermediate/advanced/expert)
- Contributor leaderboard (top 5)
- Market statistics panel
- Bid button + reward display

### Phase 5: Mobile Optimization
- All new components use responsive grid (grid-cols-1 md:grid-cols-2 lg:grid-cols-3)

### Phase 6: Scroll Animations
- ScrollReveal component using IntersectionObserver
- Supports direction: up/left/right, custom delay
- Wrapped homepage sections: LiveStatsTicker, CTECycle, Features, Ecosystem, TrustSection

### Phase 7: Globe Interaction
- Click any ecosystem node to select and show tooltip
- Hover detection with raycaster
- Bottom legend with active state highlighting
- Auto-rotate orbits + pulsing nodes

### Navigation Changes
- Removed "节点" button from nav bar
- Added "💼 劳动力市场" link to `/labor`
- Added "🤖 AI 分身" link to `/avatar`
- Admin "⚙️ 后台" only visible when wallet is connected
- Cross-page hash navigation fix: clicking `#nodes`/`#dns` from non-homepage now navigates to `/#sectionId`

### Team Section Avatars
- Replaced all emoji/initial placeholders with 10 unique sci-fi NFT-style SVG avatars
- Patterns: Circuit mask, hex crystals, geometric mask, digital grid, pulse rings, data stream, core node, matrix code, neural web, void star
- Pure SVG data URI — no external images, zero copyright risk

### i18n Updates (2026-06-25 01:34)
- Added `homepage.*` (20 keys), `avatar.*` (35 keys), `labor.*` (14 keys) to both zh-CN and en-US
- Updated CTECycle, LiveStatsTicker, TrustSection, AvatarDashboard, LaborMarket to use `t()`
- Still-outstanding hardcoded Chinese: AFCTokenPanel, EconomyModelPanel, SparkNFTPanel, LoginModal (blockchain/wallet pages)

### Navigation i18n
- Added `nav.avatar` (AI 分身 / AI Avatar), `nav.labor` (劳动力市场 / Labor Market)
- Removed `nav.nodes`
- Admin stays `nav.admin` (管理后台 / Admin Panel)

### Nav Items Order
home | whitepaper | poueEngine | web4Dns | labor | afcWallet | avatar | ecosystem | column | team | admin(if connected)

### Technical Learnings
1. Vercel monorepo build: rootDirectory must be `packages/frontend`, install command must `cd ../..` to root for pnpm workspace
2. Vercel cannot resolve `@/` path aliases in monorepo → use relative imports
3. `sourceFilesOutsideRootDirectory: true` is critical for monorepo deployments
4. GitHub auto-deploy was unreliable — manual `vercel deploy --prod` was the reliable path
5. Dual TeamSection components exist: `components/Team/TeamSection.tsx` (original) and `components/Sections/TeamSection.tsx` (detailed)
6. User is serious about the project — willing to push for quality improvements iteratively
